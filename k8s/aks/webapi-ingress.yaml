apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nginx
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: ecoders.ca
      http:
        paths:
          - path: /
            backend:
              serviceName: ecoders-web-service
              servicePort: 80